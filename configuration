from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    APP_NAME: str = "School Timetable"
    DATABASE_URL: str = "postgresql+asyncpg://postgres:postgres@localhost:5432/timetable"

    JWT_SECRET: str = "CHANGE_ME_SUPER_SECRET"
    JWT_ALGO: str = "HS256"
    ACCESS_TOKEN_EXPIRE_MINUTES: int = 8 * 60  # 8h

    class Config:
        env_file = ".env"

settings = Settings()

